@model IEnumerable<ProjetGFormation.Models.LesFormations>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.titre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.titre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LesFormationsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LesFormationsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LesFormationsId })
        </td>
    </tr>
}

</table>
